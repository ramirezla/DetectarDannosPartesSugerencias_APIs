services:
  - type: web
    name: api-dannos
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "uvicorn api.app:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"